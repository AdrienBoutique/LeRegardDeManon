services:
  postgres:
    image: postgres:16
    container_name: lrdm_postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: lrdm
      POSTGRES_PASSWORD: lrdm_pass
      POSTGRES_DB: leregarddemanon
    ports:
      - "5433:5432"
    volumes:
      - lrdm_pgdata:/var/lib/postgresql/data

volumes:
  lrdm_pgdata:
